# 2026-06-08 작업 로그

## 휴점 등록 기능 신설 (`6f1b349`)
- 매장 사이드바 → 근태관리 → **🏪 휴점 등록** 신규 메뉴
- 익월 캘린더에서 휴점일 선택 → upsert (매니저·부매니저 누구나)
- 표시 정책: 휴점 + 미출근 = `🏪 휴점`, 휴점 + 출근 = `확인필요`
- 연동: 사이드바 출퇴근 카드 / 본사 출퇴근현황·이력 탭 / 급여 엑셀(payrollExcel)

## 본사 근태관리 기간 피벗 조회 (`d21a132`, `1a67acc`, `7f2e2f3`)
- 출퇴근현황 탭을 단일 날짜 → from~to 기간 + 매니저×일자별 컬럼 피벗으로 개편
- 셀 표시: 출근/퇴근 시각 두 줄 | 휴점(보라) | 휴무(파랑) | 미체크(빨강)
- 같은 점포·지점 행 rowSpan 세로 병합 (정렬: 점포 → 지점 → 매니저 → 부매니저)
- 엑셀 다운로드도 동일 피벗 양식으로 변경 + 셀 병합

## 본사 출퇴근 시각 셀 직접 편집 (`b57b927`)
- 본사 담당자·어드민: 출퇴근현황 셀 클릭 → 모달에서 시각 수정/삭제 가능
- 미체크 셀 클릭 → 신규 attendance row 생성도 가능 (store_account_id 매칭)

## 본사 택배요청 표 UI/기능 강화
- 그룹(같은 거래) rowSpan 셀 병합 — 날짜/매장/배송지 한 번만 표시 (`511b8c1`, `ea89952`)
- 송장번호 그룹 단위 표시 + [🔍 조회] 버튼 분리 (`9542841`)
- 발송완료 탭에 기간·점포·지점 필터 + 최근 30일/3개월 버튼 (limit 50→500) (`6a269c0`)

## 본사 택배요청 — 특판 택배·용차 탭 신설 (`0dd3f8b`)
- 메인 탭 3개로 재구성: 📦 매장 본사요청 | 🏭 특판 택배 | 🚚 특판 용차
- 신규 컴포넌트 `HQDeliveryBizView.jsx`
  - 특판 택배: 발송 대기/완료, 엑셀 다운로드(ID+송장빈칸) + 송장 업로드(ID 기반), [🔍 조회] CJ대한통운
  - 특판 용차: 송장 컬럼 없음, [✓ 발송처리]만으로 완료

## 특판매출
- 수정 시 매출일도 인라인 변경 가능 (`28e2e58`)
- 입력 시 공급가 우측 [🎁 증정] 토글 — 자동 0원 설정 (`d8d1d22`)

## SCM 권한 신설 (`21cebc3`, `06b57ac`)
- `profiles.role = 'scm'` 신규 등급
- 사이드바에 본사 택배요청 한 메뉴만 표시 (다른 메뉴/섹션 모두 숨김)
- 다른 페이지 접근 시도 시 자동 hq_delivery_request로 리다이렉트
- 계정: `scm@kbh.kr` / `123456` (3글자 'scm' 로그인 가능)
- 빌드 fix: useEffect를 early return 이전으로 이동 (react-hooks/rules-of-hooks)

## DB 변경 (Supabase에서 이미 실행 완료)
```sql
-- 특판 발송 관리 컬럼
ALTER TABLE biz_sales
  ADD COLUMN IF NOT EXISTS delivery_status text DEFAULT 'pending',
  ADD COLUMN IF NOT EXISTS dispatched_at   timestamptz,
  ADD COLUMN IF NOT EXISTS dispatched_by   uuid REFERENCES profiles(id),
  ADD COLUMN IF NOT EXISTS tracking_number text;

-- SCM 사용자 등록 (Auth + profiles)
UPDATE profiles
SET role='scm', job_title='SCM담당자', name='SCM담당자', approved=true
WHERE email = 'scm@kbh.kr';
```

## 데이터 삭제 (DB만)
- 김경자B (롯데/대전점, 기타근무자) attendance row 4건 삭제
- **6/7 이전 sales 모두 삭제** (84건) + biz_sales 모두 삭제 (5건) — 운영 시작 전 테스트 데이터 정리
- **본사 택배요청 데이터 전체 삭제** — `DELETE FROM sales WHERE delivery_type='hq'` — 테스트 데이터 정리

## 다음 작업 (대기)
- 특판매출 입력 시 자동으로 `delivery_status='pending'` 들어가는지 확인 (DB DEFAULT 적용됐으니 정상이어야 함) — 사용자가 등록 후 본사 택배요청 → 특판 택배·용차 탭에서 보이는지 검증 필요
- SCM 외 추가 권한 세분화 (필요 시)
- 매장 PC 시계 자동 동기화 안내 (기존 권장 사항)
