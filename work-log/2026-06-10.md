# 2026-06-10 작업 로그

## 매출 입력 — 적립금사용 정책 재정의
- 적립금 사용 후 [카드] 클릭 시 payment가 '적립금사용'으로 덮어쓰이는 버그를 차단하고 매출조회 표시 보정 (33ae489)
- 사용자 의도(`카드+적립금 동시 결제`)에 맞춰 자동 변환을 되돌리고 결제와 적립금을 분리 표시 (6c2cc4a)
- 결제 컬럼에서 '적립금사용'을 빼고 별도 컬럼/버튼으로 분리, 폭 220+110으로 그리드 재구성 (ef9fa68)
- 매출조회의 결제 컬럼에서 잘못 저장된 `payment='적립금사용'` 뱃지 숨김 처리 (fe17d72)
- newLine 기본값 payment=''로 변경 + 결제수단 미선택 시 저장 차단 (b9e9048)
- 적립금사용 모달 confirm 시 회원적립 영역이 비어 있으면 같은 회원으로 자동 동기화 (7460627)

## 매출조회 — 트랜잭션 그룹화 + 표시 정비
- 본사 매출조회 판매내역을 결제 묶음(같은 sold_at + 매장 + 지점 + 고객 + created_at 60초 윈도) 단위로 그룹화, 상세보기 모달 추가 (b7b37be)
- 매장 매출조회도 같은 정책으로 그룹화, 인라인 펼침 + 라인 상세 토글 추가 (8dae6b8)
- 본사 매출조회 필터에 점포 옆 '지점' 드롭다운(종속) 추가 (39dc797)

## 본사 택배요청 엑셀
- 매장 본사요청 31컬럼 양식의 18번 주문자명에 매장명+지점 자동 입력 (d36107d)
- 특판 택배 발송대기에 동일 31컬럼 양식 다운로드/송장 업로드 추가, SIDB: prefix로 매장(SID:)과 구분 (d36107d)

## 상품관리
- 상품코드/ERP코드 중복 검증 + ERP코드 필수 + 판매 이력 있는 상품 삭제 시 친절 안내 (6adad80)
- 중복 경고 메시지 "이미 [상품정보] 동일한 상품이 등록되어있습니다"로 통일, 원가/판매가 필수, 파일 업로드에도 동일 검증 (7c09ebd)

## 본사 발주진행
- '매장 재고요청' 탭 신설 (`StockRequestsAdminView`), 알림에서 발주진행 + 해당 탭으로 라우팅 (`localStorage hint`) (317b52d)
- 매장·지점 단위 그룹화, 매장 그룹 row 클릭으로 라인 상세 인라인 펼침 (a810d5d)
- 펼침된 라인의 수량 인라인 편집 — 변경 시 [저장] 노출, pending만 편집 가능 (1291b62)

## 반품 회계 처리 정책 변경 (POS 표준)
- 기존: 원본 매출의 `returned_qty`를 증가시켜 원본 매출일의 effAmt 차감
- 신규: 원본 row는 손대지 않고 반품 시점에 음수 매출 row INSERT (sold_at=반품일, price=-원본가, payment='반품')
- `returned_qty`는 의미 변경 — 매출 차감용 → 이중 반품 방지 잠금용으로만 유지
- SalesReturnPage handleReturn / handleManualReturn, SalesListPage / MgrSalesViewPage effQty/effAmt 일괄 재정의 (c622e97)

## SCM 권한
- 매장 본사요청 송장 업로드 실패 사유(RLS 등)를 토스트에 노출하도록 처리 (36cae4a)

## DB 사이드 변경
- `order_requests` 테이블에 `quantity`, `memo`, `request_date`, `created_by` 컬럼 추가 (스키마 캐시 reload)
- 매출 보정 SQL — id 357 / 388 적립금 미반영 price 보정 (`price = price - 10000/quantity`)
- 매출 보정 SQL — id 523 결제수단 보정 (`payment='적립금사용'` → 실제 결제수단으로 UPDATE)
- 중복 상품 병합 SQL — id 570 → 546 병합 (sales 등 product_id 가진 8개 테이블 일괄 UPDATE 후 DROP)
- RLS 정책 "sales 수정" 수정 — `profiles.role = 'scm'` 조건 추가하여 SCM의 송장 업로드 허용
- 반품 회고 마이그레이션 — `returned_qty>0` 원본 1건에 대해 음수 매출 row 1건 생성(A), 기존 수기 반품 4건의 `price` 음수 변환(B), 총 5건 정리

## 미해결 / 다음 작업 대기
- 중복 상품 추가 정리 — 사용자 결정 대기:
  - 매출 있고 KEEP 명확: id 568(알부민), id 556(비타민D3)로 병합 후보 ✅ 권장만 제시한 상태
  - 매출 동률 (석류100 544 vs 567)
  - 매출 0 단순 정리 그룹 (쇼핑백/프리박스/메이준/코코넛오일/석류콜라겐젤리)
  - ERP코드 분리 필요 그룹 (공단보자기 408/489, 온유 오메가3 248/382)
- 반품 표시 UI 강화 — 음수 매출 row를 빨간색/반품 뱃지로 명확히 강조 (현재는 일반 row처럼 표시)
- 트랜잭션 그룹화 화면에서 반품(음수 매출) row가 어떻게 묶이는지 케이스 점검
