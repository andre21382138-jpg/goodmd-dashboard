# 2026-06-05 작업 로그

## 출퇴근 / 근태
- 매장 사이드바 출퇴근 카드에서 시각 숨김, `✓ 출근`/`✓ 퇴근`만 표시 (`5df73cd`)
- 출퇴근 체크 토스트 메시지에 한국시간 기준 날짜 표시 — `'2026년 6월 5일 출근 체크되었습니다 ✅'` (`c43da82`)
- `attendance.work_date`를 DB 서버 시간(KST)에서 자동 설정하도록 변경 — 매장 PC 시계 오류 대응 (`8055997`)
- 본사 근태관리 출퇴근현황 탭에 엑셀 다운로드 버튼 추가 (`a906c43`, `d0f670a`)
  - 컬럼: 날짜 / 점포명 / 지점 / 직책 / 이름 / 출근시간 / 퇴근시간
  - 미체크 표기 통일 (출근·퇴근 모두 `미체크`)

## 특판매출
- 특판매출 행 수정·삭제 기능 (본사·어드민 한정) (`9e5575a`)
  - 인라인 편집: 수량 / 공급가 / 배송방법 / 메모
  - 매출일·업체·상품은 변경 빈도 낮아 삭제 후 재입력 정책

## 본사 택배요청 → 송장번호 등록·표시 + CJ 추적
- 다운로드 엑셀의 '공란' 컬럼(24번)에 `SID:<sale_id>` 자동 저장 → 업로드 시 매핑 키
- `[📤 송장 업로드]` 버튼 추가, 자동 파싱 → `tracking_number` + `dispatched` 일괄 처리 (`e9888f2`)
- 송장번호 토스트에 미입력 건수 안내 — `처리 5건 / 미입력 5건은 대기 그대로` (`5e26ebc`)
- 표에 송장번호 컬럼 추가, 클릭 시 `https://tracker.delivery/#/kr.cjlogistics/{송장}`로 새 창 → **CJ대한통운 배송조회**
- tracker.delivery 실제 송장(589158224585)으로 작동 검증 완료

## 데이터 보정 / 점검
- 롯데백화점 포항점 장현숙 매니저 6/5 출근체크가 6/4로 잘못 기록된 건 보정 — id=141 `work_date` 6/4→6/5, `clock_in` 6/5 09:00 KST로 update
- 6월 1일부터 운영 시작이라 5월 attendance 27건은 사전 테스트 데이터지만 그대로 유지 결정 (5월 통계 시 영향)
- 장현숙 6/1·6/3·6/5 출근일 일치 확인 — 추가 보정 불필요

## DB 변경 (Supabase에서 이미 실행 완료)
```sql
-- 매장 PC 시계 오류 대응
ALTER TABLE attendance
  ALTER COLUMN work_date SET DEFAULT (NOW() AT TIME ZONE 'Asia/Seoul')::date;

-- 송장번호 컬럼
ALTER TABLE sales ADD COLUMN IF NOT EXISTS tracking_number text;
```

## 매장 PC 시계 점검 안내
매장 PC가 어제 날짜로 설정돼 있으면 출퇴근 시각·매출 입력 sold_at 등이 그 잘못된 날짜·시간으로 기록됨. work_date는 DB 서버 시간으로 자동 설정되어 분석은 정상이지만, 매장에 다음 안내 권장:
> Windows 설정 → 시간 및 언어 → 날짜 및 시간 → **"시간 자동 설정" 켜기**

## 다음 작업 (대기)
- 매장 PC 시계가 잘못된 상태에서 `clock_in` 시각·`sold_at` 등이 어긋나는 문제 — 필요 시 서버 시간 RPC 함수 도입
- 단계 B: 본사가 매장별 판매내역 입력·수정 (5/28부터 이어짐, 보류 상태)
- 매니저 현황 페이지 `manager_mgmt` 사이드바 메뉴 노출 위치 결정
- `biz_partners` 미사용 테이블 정리
