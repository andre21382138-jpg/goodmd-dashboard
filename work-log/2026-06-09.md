# 2026-06-09 작업 로그

## 본사 매출조회 (SalesListPage)
- 매장별 판매내역 집계 뷰(매트릭스) 추가 + 행 클릭 시 드릴다운 모달 (a2dd67c)
- 매출목록 표 '출고방식' 셀이 잘리는 문제 해결 (a2dd67c)
- `drillRows` 변수 중복 선언으로 발생한 빌드 에러 → `drillStoreRows`로 분리 (3ddf3ac)
- 6/8 조회 시 엑셀에 6/7 데이터가 섞이는 KST/UTC 문제 — 1차 시도(필터에 +09:00) 적용 (c993b5e)
- 위 시도가 date 컬럼에서는 과보정 → 필터는 원복하고 엑셀 Date 객체를 KST 정오로 고정 (97e7249)

## 출퇴근 / 근태 UI
- 출근/퇴근 토스트 문구를 "(날짜) 출근/퇴근체크가 완료되었습니다." 형식으로 변경 (590de68)
- 사이드바 좌측상단 출퇴근 시간 표시 제거 (590de68)
- 매장 매니저용 근태관리 화면: 출근/퇴근 시간을 "✓ 체크완료" / "미체크" 텍스트로 변경, 근무시간 컬럼·총 근무시간 통계 삭제 (a558b11)

## 신규 배포 자동 감지 (매장 강제 새로고침 불필요)
- `prebuild` npm 스크립트로 빌드 시 `public/build.json`에 ISO timestamp 자동 작성 (403a85e)
- 클라이언트가 5분 주기 + 탭 포커스 시 build.json 폴링 (403a85e)
- 1차 구현: 새 빌드 감지 → 토스트 안내 후 2.5초 뒤 자동 reload (403a85e)
- **사용자 피드백 반영**: 작업 중 자동 reload 시 입력 데이터 손실 위험 → 자동 reload 제거, 상단 고정 배너 + [지금 새로고침] / [나중에] 버튼으로 변경 (6b2daf5)

## 미해결 / 다음 작업 대기
- 특판매출(biz_sales) 입력 시 본사 택배요청 발송대기 탭으로 자동 라우팅되는지 실 환경 검증 (테스트 데이터 삭제 후 실측 필요)
- 새 배포 감지 배너의 실제 작동은 다음 배포부터 확인 가능 (오늘 배포 이후 다음 배포가 첫 트리거)
