# 2026-05-08 작업 정리

## 오늘의 큰 흐름

매장발주 워크플로우 완성 — 매장 마스터 데이터 관리 + 외부 택배 양식 엑셀 다운로드까지 end-to-end 구축.

```
본사 발주진행 → 매장 발주확인 → 본사 발주현황·다운로드 → 매장 입고확인
   (이미 있음)     (이미 있음)        (오늘 추가)         (이미 있음)
```

---

## 1. 매장매출 엑셀 다운로드 양식 정렬 (commit `4f3acf1`)

본사 매장매출 페이지의 엑셀 다운로드 결과물을 외부 `매출raw` 시트와 일치하도록 13개 항목 변경.

- 글씨체: Gulim 9 / Arial 10 (헤더 12열만 맑은 고딕 10 굵게)
- 헤더 배경: 최종금액 컬럼만 노란색(`FFFFFF00`), 나머지 없음
- 컬럼 폭, numFmt(`@` / `[$-412]yyyy-mm-dd` / `0_);[Red](0)` / `#,##0` / `0%`)
- 헤더 줄바꿈(`전표\n유형`, `매출\n수량`)

## 2. 반품 교환 통합 처리 — **취소**

설계와 구현 계획까지 진행했으나(spec `fd1511e` + plan `94759ad`), 사용자 결정으로 작업 자체를 취소.

- DB 컬럼 `sales.exchange_from_sale_id` 도 다시 DROP함
- spec/plan 문서는 main에 남아있음(참고용)
- 매장매출 엑셀 다운로드 기능은 그대로 유지(별개 기능이라 영향 없음)

## 3. 매장 정보 페이지 (Phase 1) (Merge `cfdd765`)

본사 사이드바에 매장별 마스터 데이터 관리 페이지 신설.

- 신규 테이블: `store_addresses` (id, store_name, branch_name, shopping_mall_id, postal_code, address, recipient_phone)
- 사이드바 메뉴: 본사 → `📍 매장 정보`
- 신규 페이지: `src/pages/store/StoreInfoPage.jsx` (262줄)
  - 매장 목록 (STORE_MAP × store_addresses 조인)
  - 검색·필터, ✅/⚠️ 상태 표시
  - 편집 모달 (upsert)
  - 엑셀 일괄 import (헤더 검증 + upsert)

## 4. STORE_MAP 대량 정리

매니저(profiles) 데이터 + 사용자가 가진 외부 양식(`☆팔레오 백화점주.xlsx`, `매장발주_*.xls`)을 대조해 STORE_MAP 정렬.

| 백화점 | 변경 |
|---|---|
| 롯데백화점 | +6 (광복/구리/대전/동래/미아/일산/부산본점), -8 (천호/강남/잠실/노원/본점/인천/광주/기타), 건대시티점 → 건대스타시티점 복구 |
| 신세계백화점 | +1 (의정부점), -7 (강남/본점/센텀시티/광주/충청/하남/기타) |
| 현대백화점 | +1 (충청점), -10 (더현대서울/목동/무역센터/천호/신촌/압구정본점/판교/대구/울산/기타) |
| AK백화점 | -4 (분당/원주/홍대/기타) |
| 갤러리아 | -2 (명품관/기타) |
| 그린푸드 | placeholder 제거 → +10 (현대 입점 매장: 더현대서울/현대 천호/신촌/목동/울산/대구/미아/디큐브시티/킨텍스/중동) |
| 농협_SHOP | 점포명을 `농협_SHOP` placeholder → `남창원점`으로 정렬 |
| 특판 | placeholder 제거 → +4 거래처(와이비팜 한용수/김성현, 파마스퀘어, 사러가) |
| 신규 백화점 | +4 (영남실업, 동화상사, 창원지점(박경원님), 중소기업명품마루) |

## 5. 매장 import 데이터 채우기

- `매장정보_import_template.xlsx`를 매장발주 + 점간이동용 시트 데이터를 합쳐 자동 생성
- 사용자가 일괄 import 후 빠진 매장 정보 직접 편집으로 보충
- 그린푸드 6개 매장은 `현대 + 점포명` prefix 패턴으로 정렬 (DB UPDATE)
- profiles 매니저 mismatch 정리: 농협 → 농협_SHOP, 그린푸드_현대대구점 → 현대 대구점
- 건대 점포명 정렬: profiles와 store_addresses 모두 `건대스타시티점`으로 통일 (DB UPDATE + STORE_MAP 복구)

## 6. 메뉴 재구성 (commit `059b98c`)

- 매장 정보 → **매장주소정보**로 개명
- 본사 사이드바 top-level에서 **재고관리 sub**로 이동
- 발주관리 → **발주진행**으로 라벨 변경
- 신규 메뉴: **발주현황** (placeholder)

최종 재고관리 sub 구조:
```
재고관리 ▶
  센터재고
  매장재고
  발주진행
  발주현황         ← 신규
  매장주소정보     ← 이동·개명
```

## 7. 발주서 엑셀 다운로드 (Phase 2) (Merge `06c9eac`)

본사 발주현황 탭에 매장발주 양식 31컬럼 xlsx 한 번에 다운로드 기능 추가.

### 변경 사항

- DB: `purchase_orders.exported_at TIMESTAMPTZ` + 부분 인덱스
- `src/lib/constants.js` — `ORDER_CONSTANTS` 신규 (송장번호=유통2팀, 채널=기타_유통2팀_매장발주, 주문자명=한국생활건강(팔레오본사), 주문자 연락처=070-5117-5677)
- `src/pages/order/PurchaseOrderHQPage.jsx`:
  - 모듈 스코프 헬퍼 `exportPurchaseOrders()` 추가 — 페이징 fetch + ExcelJS 빌드 + dlBlob + exported_at 일괄 update
  - 발주현황 탭 상단에 `📥 매장발주 엑셀 다운로드` 버튼
  - 매장 마스터 미설정 시 confirm 다이얼로그
  - 발주현황 표 status 컬럼에 `✓ 출하됨` 배지 + 다운로드 시간 tooltip

### 다운로드 대상

`status IN ('requested', 'rerequested') AND exported_at IS NULL`인 발주들. 다운로드 후 자동으로 `exported_at = NOW()` 기록되어 다음 다운로드에선 자동 제외.

### 양식 검증

mock 데이터로 샘플 xlsx 생성하여 31컬럼 구조와 데이터 매핑 확인 완료 (`매장발주_샘플.xlsx`).

---

## 머지 커밋 정리 (오늘)

| 시간 | 머지 | 내용 |
|---|---|---|
| - | `8c116c3` | 매장매출 결산 양식 엑셀 다운로드 |
| - | `4f3acf1` | 매장매출 엑셀 양식 외부 매출raw 일치 |
| - | (취소) | 반품 교환 통합 처리 |
| - | `cfdd765` | 매장 정보 페이지 (Phase 1) |
| - | `059b98c` | 매장정보 → 매장주소정보 + 재고관리 이동 |
| - | `06c9eac` | 발주서 엑셀 다운로드 (Phase 2) |

## DB 스키마 변경 (오늘)

| 테이블 | 변경 |
|---|---|
| `sales` | `exchange_from_sale_id` 추가 후 다시 DROP (반품 교환 취소) |
| `store_addresses` | **신규 테이블** (id, store_name, branch_name, shopping_mall_id, postal_code, address, recipient_phone, updated_at) + UNIQUE(store_name, branch_name) |
| `purchase_orders` | `exported_at TIMESTAMPTZ` 추가 + 부분 인덱스 |

## 다음에 할 일 / 보류 항목

- **handleExport duplicate fetch 정리** (Important): 발주현황 다운로드 시 preview query와 export query가 중복. exportPurchaseOrders에 콜백 inject 또는 args로 데이터 패싱.
- **exported_at 비원자적 update** (Important): 다운로드 성공 후 update 실패 시 메시지 sharper.
- **발주현황 placeholder 페이지** 실제 기능 구현 (구현 예정 상태)
- **매니저 매칭 mismatch 일부 잔존**: profiles와 store_addresses 사이 사소한 불일치 가능 (이번 작업으로 대부분 정리됨)
